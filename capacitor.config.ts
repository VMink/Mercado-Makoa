import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mercadomakoa.mercadomakoa',
  appName: 'Mercado Makoa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
